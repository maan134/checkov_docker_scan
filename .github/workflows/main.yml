name: Checkov Docker Scan

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  checkov-docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Install Checkov (v2.1.97)
        run: pip install checkov==2.1.97

      - name: Build Docker Image
        run: docker build -t myimage:latest .

      - name: Scan docker file
        run: |
          checkov -f Dockerfile --all-checks --output sarif --output-file-path checkov-report.sarif

      - name: Upload SARIF artifact
        uses: actions/upload-artifact@v4
        with:
          name: checkov-report
          path: checkov-report.sarif

      - name: Upload SARIF as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: checkov-report
          path: checkov-report.sarif
